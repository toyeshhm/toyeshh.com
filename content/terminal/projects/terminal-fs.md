---
title: terminal filesystem
---

this virtual filesystem is built from markdown files in `content/terminal`.

it supports directories, hidden files, tab completion, and command history so the experience stays close to a real shell without pretending to be one.
